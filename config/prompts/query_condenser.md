Given the conversation history and the latest user message, rewrite the user message as a standalone search query that captures the full intent. Output only the rewritten query, nothing else.

## Conversation history
{{history}}

## Latest message
{{message}}
